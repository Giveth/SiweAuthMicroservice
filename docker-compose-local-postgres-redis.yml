version: '3.3'

services:
  siwe_microservice-postgress:
    image: postgres:14.2-bullseye
    container_name: siwe_microservice-postgress
    environment:
      - POSTGRES_DB=siwe_microservices
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - PGDATA=/var/lib/postgresql/data_new/pgdata
    ports:
      - "5443:5432"
    volumes:
      - siwe_microservice-postgres-data:/var/lib/postgresql/data

  siwe_microservice-postgress-test:
    # CAUTION: Running tests will delete all records of this db, so just use this container for test
    # For running application use above container
    image: postgres:14.2-bullseye
    container_name: siwe_microservice-postgress-test
    environment:
      - POSTGRES_DB=microservices
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - PGDATA=/var/lib/postgresql/data_new/pgdata
    ports:
      - "5444:5432"
    volumes:
      - siwe_microservice-postgres-data-test:/var/lib/postgresql/data

  siwe_microservice-redis:
    # it's better to not using latest tag, maybe latest tag have some breaking changes
    image: bitnami/redis:5.0.9-debian-10-r142
    container_name: siwe_microservice-redis
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
    restart: always
    ports:
      - "6489:6379"
    volumes:
      - siwe_microservice-redis-data:/bitnami/redis/data

volumes:
  siwe_microservice-postgres-data:
  siwe_microservice-postgres-data-test:
  siwe_microservice-redis-data:
